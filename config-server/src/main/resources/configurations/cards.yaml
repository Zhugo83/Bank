server:
  port: 8070
eureka:
  instance:
    hostname: localhost
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/ms_demo_cards?useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: Password
  jpa:
    hibernate:
      ddl-auto: create
    database: mysql
    database-platform: org.hibernate.dialect.MySQL8Dialect
  management:
    endpoints:
      web:
        exposure:
          include: "*"